<h1>Create category</h1>
<%= form_for [:manager, @category] do |f| %>  
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name, required: true %>
    </p>
    <p>
      <%= f.submit class: "btn btn-lg btn-primary" %>
    </p>
     
<% end %>
 
<%= link_to 'Back', manager_index_path %>