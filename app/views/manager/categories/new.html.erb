<h1>Create category</h1>
<%= form_for [:manager, @category] do |f| %>  
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.submit %>
    </p>
     
<% end %>
 
<%= link_to 'Back', menus_path %>